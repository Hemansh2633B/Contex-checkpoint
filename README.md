# Contex-checkpoint
context-checkpoint is an AI Agent Skill that prevents chat loss by saving conversations as structured, resumable .md checkpoints. It compresses context, preserves key state, supports cross-model continuation, encryption, versioning, and instant resume—turning AI chats into portable, executable workflows.
